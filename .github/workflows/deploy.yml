name: Deploy to Fornex

on: push

jobs:
  deploy_to_fornex_ftp:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@master

      - name: Copy files to ftp
        # uses: appleboy/scp-action@master
        uses: SamKirkland/FTP-Deploy-Action@3.1.1
        with:
          # host: ${{ secrets.SERVER_HOST }}
          # username: ${{ secrets.SERVER_USERNAME }}
          # password: ${{ secrets.SERVER_PASSWORD }}
          # port: ${{ secrets.SERVER_PORT }}

          host: ftp.f59436.hostus01.fornex.host
          username: ftp_fornex@falkov.site
          password: 12Gamer1955_ftp
          port: 21
          rm: true
          source: "./*"
          # target: "public_html/falkov.site/webnotes"
          # target: "/home/f59436/public_html/falkov.site/webnotes"
